{% if before %}
    <a class="prev" href="{{ before.slug }}.html">
        &laquo; {% if before.number %}{{ before.number }} - {%endif %} {{ before.chapter }}
    </a>
{% endif %}
{% if after %}
    <a class="next" href="{{ after.slug }}.html">
        {% if after.number %}{{ after.number }} - {% endif %} {{ after.chapter }} &raquo;
    </a>
{% endif %}
